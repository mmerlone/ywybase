# Environment
NODE_ENV=development

# Logging Configuration
# Options: debug, info, warn, error
LOG_LEVEL=info


# Supabase Configuration
# Get these values from: https://supabase.com/dashboard
# 1. Create a new project or select existing one
# 2. Go to Project Settings > API
# 3. Copy the Project URL and Anon Key
NEXT_PUBLIC_SUPABASE_URL=https://your_supabase_project_id.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key

# Supabase Development (Optional)
# Used for local development callbacks
NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=http://localhost:3000

# Supabase Project ID (Required for type generation)
# Extract from your Supabase URL: https://[PROJECT_ID].supabase.co
# Example: If URL is https://abc123def.supabase.co, then PROJECT_ID is abc123def
SUPABASE_PROJECT_ID=your_supabase_project_id

# Supabase Secret Key (Optional - Server-side only)
# Get from Project Settings > API > Database Settings > Password
# WARNING: Never expose this key in client-side code
SUPABASE_SECRET_KEY=your_supabase_secret_key

# Supabase Database Password (Optional - Server-side only)
# Get from Project Settings > API > Database Settings > Password
# WARNING: Never expose this key in client-side code
SUPABASE_DB_PASSWORD=your_supabase_db_password


# Sentry Configuration

# Sentry Error Tracking (Optional)
# Get from: https://sentry.io
# 1. Create a new organization/project
# 2. Go to Settings > Client Keys (DSN)
# 3. Copy the DSN value (full URL including https://)
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn

# Sentry Auth Token (Optional - For deployment)
# Get from Sentry > Settings > Auth Tokens
SENTRY_AUTH_TOKEN=your_sentry_auth_token


# IP Geolocation API (Optional)
# Get from: https://ipgeolocation.io
# 1. Sign up for free account
# 2. Go to Dashboard > API Keys
# 3. Copy your API key
IPGEOLOCATION_API_KEY=your_ipgeolocation_api_key


# Security Configuration
# CSRF Protection Secret (Required in production)
# Generate a secure random string (32+ characters)
# Example: openssl rand -hex 32
CSRF_SECRET=your_csrf_secret_32_chars_minimum

# Upstash Redis (Required for production rate limiting with multiple instances)
#
# Setup (one-time, per developer):
#   1. Install Vercel CLI:  pnpm i -g vercel
#   2. Link the project:    vercel link
#   3. Pull env vars:       vercel env pull
#
# The Vercel/Upstash Redis integration (https://vercel.com/marketplace?category=storage&search=redis)
# automatically injects the following variables into your Vercel project.
# `vercel env pull` copies them into your local .env.local file.
#
# REST API endpoint for the Upstash Redis database — used by @upstash/redis SDK.
KV_REST_API_URL=https://your-endpoint.upstash.io
# Full-access REST API token (read + write). Keep secret — server-side only.
KV_REST_API_TOKEN=your_kv_rest_api_token
# Read-only REST API token. Provides read-only access — keep secret (server-side only).
KV_REST_API_READ_ONLY_TOKEN=your_kv_rest_api_read_only_token
# Redis-protocol connection string (rediss://). Only needed for TCP-based clients
# such as ioredis. NOT used by this project (@upstash/redis uses the REST API above).
KV_URL=rediss://default:your_kv_rest_api_token@your-endpoint.upstash.io:6379
# Alias for KV_URL — also injected by the integration. NOT used by this project.
REDIS_URL=rediss://default:your_kv_rest_api_token@your-endpoint.upstash.io:6379


# Vercel-managed variables (injected automatically at runtime — do not set manually)

# Short-lived OIDC token issued by Vercel on each invocation for service-to-service auth.
# This is rotated automatically; never hard-code or commit a real value.
VERCEL_OIDC_TOKEN=vercel_managed_do_not_set_manually